# 灵感卡片墙 · 产品设计文档（v2 · 秋风落叶版）

> 一款纯前端单页小工具，用于随手记录灵感、好句、语录。卡片化作不同种类的秋叶，沿落叶轨迹从屏幕上方缓缓飘落，沉浸式重温每一次灵光一闪。

---

## 1. 产品概述

### 1.1 一句话定位
把零散的灵感、句子、语录，写成一片片会飘落的秋叶——写下来，随风飘起，落进心里。

### 1.2 灵感主题 · 秋风落叶
- 每张卡片**就是一片秋叶**，被裁剪为不同叶型（枫叶 / 银杏 / 梧桐 / 橡叶等），呈现秋色系的叶片颜色。
- 叶型与卡片的**标签/分类**对应：标签决定默认叶片形状（如「语录→枫叶」「读书→银杏」），用户也可手动指定。
- 卡片从屏幕上方沿随机轨迹下落：带 **sin 曲线左右摇摆**、**轻微自转**、**全局风力**影响；分为近中远三层，速度 / 大小 / 透明度递进，营造景深。
- 页面背景：**秋日金黄渐变** + 简约线条画远山 / 树 / 云 / 芦苇，整体像一幅会动的秋日插画。

### 1.3 目标用户
- 个人创作者、写作者、读书爱好者
- 习惯随手记录灵感、金句，希望记录本身带有**仪式感与治愈感**
- 喜欢轻量、好看、私密、无打扰的本地记录空间

### 1.4 核心场景
1. **随手记**：灵光一闪或读到好句子，点开底部胶囊菜单，快速填几行存下；新卡片立刻以一片新叶的姿态从顶部飘入。
2. **沉浸看**：打开页面就让叶子静静地飘。鼠标悬停某片叶，它会停在原地并放大，像拾起一片落叶仔细端详。
3. **快速找**：点击底部「搜索」输入关键词，**未命中的叶子逐渐淡出消失**，只留下命中的叶子继续飘落并高亮关键词；或切进管理面板做精确整理。
4. **整理维护**：打开管理面板，以网格视图统一编辑、删除、打标签、分类。

### 1.5 设计原则
- **轻**：纯前端单页，无后端，打开即用，数据本地存储。
- **美**：暖秋色系 + 插画线条 + 飘落动效 + 景深层次；治愈、宁静、不扁平。
- **柔**：所有动效走缓动曲线（`cubic-bezier(.2,.7,.3,1)`），不突兀，像真实秋风。
- **快**：新建即存、搜索实时、飘落流畅；操作入口集中在底部，不打断画面。
- **稳**：删除二次确认，数据本地持久化，可备份。

---

## 2. 核心功能一览

| 模块 | 功能 |
| --- | --- |
| 落叶展示 | 近/中/远三层向下飘落、左右摇摆、轻微旋转、全局风力、悬停停住并放大、循环飘落到底后从顶上重新入场 |
| 卡片外观 | 叶片形状裁剪（枫/银杏/梧桐/橡叶等 5-6 种）、秋色系配色、叶型 ↔ 标签映射 |
| 卡片管理 | 新建（编辑表单选择叶型/颜色）、编辑、删除（二次确认）、详情查看 |
| 搜索 | 全局实时搜索、未命中叶片淡出消失、命中关键词高亮 |
| 排序与分组 | 最新在前 / 随机 / 按来源分组（对应前景/中景大小分配、不同下落区域） |
| 管理面板 | 全屏或抽屉式网格视图，集中管理所有卡片，内含搜索/编辑/删除 |
| 菜单 | 屏幕底部中间**胶囊形按钮组**：搜索、排序分组、管理面板、+新建 |
| 氛围主题 | 秋日氛围切换（清晨薄雾 / 黄昏暖金 / 雨后冷黄），即背景渐变 + 线条画色温的整体变化 |
| 数据 | 浏览器本地存储 |

---

## 3. 卡片数据模型

卡片字段**完全保留 v1**，并新增一个字段 `leaf`（叶片样式）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 唯一标识，自动生成（时间戳+随机串） |
| `title` | string | 标题，必填，简短概括 |
| `body` | string | 正文，必填，可较长（支持多行纯文本） |
| `source` | string | 来源，选填，如书名/作者/网址 |
| `tags` | string[] | 标签数组，选填，如 `["语录","读书"]` |
| `leaf` | object | **新增** · 叶片外观配置（见下） |
| `createdAt` | number | 创建时间戳，自动记录 |
| `updatedAt` | number | 最后修改时间戳，自动更新 |

> 移除 v1 的 `bg` 字段，改为 `leaf`；叶片本身的颜色取代了原背景色。

### 3.1 叶片外观配置 `leaf`
```jsonc
{
  "type": "maple",          // 叶型：maple 枫 | ginkgo 银杏 | platanus 梧桐 |
                            //       oak 橡叶 | willow 柳叶 | birch 白桦叶
  "color": "#e07a2c",       // 叶片主色（秋色系）
  "shade": "#c45a1a"        // 阴影色/描边色（用于叶脉、描边，通常为主色暗一级）
}
```

### 3.2 叶型 ↔ 标签默认映射（新建时自动选，用户可手动改）
| 标签（首匹配） | 默认叶型 | 默认主色 |
| --- | --- | --- |
| `语录` | 枫叶 maple | 橙红 `#d94e2f` |
| `读书` / `读书笔记` | 银杏 ginkgo | 金黄 `#e5b93a` |
| `灵感` | 梧桐 platanus | 橙褐 `#c77b34` |
| `写作` | 柳叶 willow | 赭黄 `#b98832` |
| `摘抄` / `摘录` | 橡叶 oak | 棕红 `#a9532a` |
| 其他 / 无标签 | 白桦叶 birch | 浅金 `#d9a441` |

### 3.3 秋色系预设（新建表单的颜色快速选择）
提供 12 种左右的秋季颜色盘预设：橙红、朱砂、金黄、赭石、棕褐、秋香绿、浅金、暗橙、珊瑚橙、枫红、橘黄、琥珀。

---

## 4. 详细功能设计

### 4.1 落叶展示（舞台）

**布局**
- 整屏作为「飘落舞台」，从顶部到底部是完整的飘落通道；不设纵向居中，顶 → 底全路径可见。
- 背景是**浅金到暖橙的渐变天空**，底部/远景叠加简约线条画（远山、树、芦苇、云）。
- 叶片落在屏幕底部约 10-15% 区域时，自动淡出并回到顶部重新入场，形成循环不落的秋日。

**分层（近中远三层 · 营造景深）**
| 层 | 比例 | 大小缩放 | 透明度 | 下落速度 | 摇摆幅度 | 卡片分配 |
| --- | --- | --- | --- | --- | --- | --- |
| 远层 | 30% 卡片 | 0.65 倍 | 0.55 | 慢（0.2px/帧） | 小（±12px） | 较旧 / 较少查看的卡片 |
| 中层 | 40% 卡片 | 0.85 倍 | 0.80 | 中（0.35px/帧） | 中（±28px） | 其余卡片 |
| 近层 | 30% 卡片 | 1.05 倍 | 1.00 | 快（0.55px/帧） | 大（±48px） | 最新修改 / 重要卡片 |

> 排序模式会影响分层分配（见 §4.5）。

**下落动效**
- 每张叶片从顶部随机 x 位置飘入（留出边距，避免贴边）。
- **左右摇摆**：使用 `sin(时间/周期 + 相位)` 叠加在 x 方向，周期与相位每张不同。
- **轻微旋转**：每张叶片带初始倾角 ±5-15°，下落过程中缓慢自转 ±10° 范围内波动。
- **全局风力**：一个缓慢变化的全局风向值（每 6-10 秒切换一次风强度 / 左右方向），叠加到所有叶片的 x 位移上，让整屏叶子有被风吹斜的感觉。
- 悬停某片叶：该片叶**立刻冻结**当前位置 / 摇摆 / 旋转，放大 1.18 倍，阴影加深，右上角浮现编辑 / 删除操作按钮。
- 移出叶片：1 秒后恢复自由下落（过渡平滑，不突兀）。

**叶子卡片外观（叶片就是卡片）**
- 整张卡片使用 SVG 叶片路径作为**裁剪蒙版（clip-path）** 或**作为背景矢量**，文字直接落在叶片表面区域。
- 叶型为：枫叶（掌状裂）、银杏（扇形）、梧桐（宽掌状）、橡叶（波浪圆齿）、柳叶（细长尖）、白桦叶（三角卵）—— 用 SVG 路径绘制。
- 颜色：使用 `leaf.color` 主色 + `leaf.shade` 阴影色，在叶脉/边缘做浅色描边增强立体感；叶片正面中央微微提亮，模拟纸面光泽。
- 文字区域限制在叶片中心较宽部分，避免文字落到叶尖 / 裂齿边缘而被裁掉。
- 标题在上、正文在中、来源 + 标签 + 日期在下方；正文超出则截断（-webkit-line-clamp），靠「点击查看详情」看全文。

### 4.2 卡片管理（创建 / 编辑 / 删除）

采用"**落叶内联快捷操作 + 独立管理面板**"模式（两者结合）。

#### 4.2.1 落叶内联操作
- 鼠标悬停某叶片时，右上角浮现操作图标：编辑（铅笔）、删除（垃圾桶）。
- 点击编辑 → 打开编辑模态弹窗。
- 点击删除 → 弹出二次确认弹窗（"确认让这片叶子飘落离开？此操作不可撤销"），确认后该叶片以加速下落 + 淡出动画消失。

#### 4.2.2 独立管理面板
- 底部胶囊中央按钮「管理」点击打开管理面板（从右侧滑入抽屉，或整屏网格覆盖）。
- 面板以**网格**形式展示所有卡片，每条含叶片缩略 + 标题、正文摘要、来源、标签、时间。
- 支持在面板内：新建、编辑、删除、搜索、按标签筛选、排序切换。
- 面板与落叶展示共享同一份数据，任一处修改实时同步。

#### 4.2.3 新建/编辑表单
表单字段：
- 标题（输入框，必填）
- 正文（多行文本域，必填）
- 来源（输入框，选填）
- 标签（标签输入器：回车添加，点击 × 删除，可选已有标签；改标签时可联动建议叶型）
- **叶片配置（取代原背景配置）**：
  - 叶型选择：6 种叶型的图标式按钮组（枫/银杏/梧桐/橡叶/柳/桦），点选即切。
  - 主色选择：12 个秋季预设色 + 自定义取色器。
  - 阴影色：自动根据主色生成（暗 25%），也可手动调。
  - 「按标签自动选叶型」开关：一键重置为默认映射。
  - 右侧实时预览区（一张真实缩放比例的叶片，填充当前设置的文字和外观）。

#### 4.2.4 删除二次确认
- 无论是落叶内联、管理面板还是详情弹窗，删除均需二次确认。
- 文案贴合主题："确认让这片叶子离开？此操作不可撤销"。
- 确认按钮用秋叶橙红 `#d94e2f` 作警示色，与取消按钮区分。

### 4.3 详情查看（模态弹窗）

- 点击叶片打开**居中模态弹窗**。
- 半透明柔金色遮罩（不再是深灰，与秋日主题匹配），可有轻微模糊。
- 弹窗整体是**放大的叶片形态**（或圆角纸张 + 角落装饰叶片元素），展示完整字段：
  - 顶部：当前叶型对应的装饰叶片角 + 标题（大号字）
  - 正文（完整，可滚动）
  - 来源
  - 标签（chip 形式，chip 颜色与标签-叶型映射一致）
  - 创建时间 / 修改时间
- 弹窗底部提供"编辑""删除"按钮，可直接在此修改。
- 点击遮罩、关闭按钮、按 ESC 均可关闭弹窗。

### 4.4 搜索

- **范围**：标题 + 正文 + 来源 + 标签，全局匹配。
- **交互**：输入即过滤，实时更新结果（无需回车）。
- **视觉表现（与 v1 关键区别）**：
  - **未命中的叶片逐渐淡出消失**（透明度 → 0，同时缓慢加速下落离开画面），不显示在舞台中。
  - **命中的叶片继续飘落**，并把命中关键词在标题/正文/来源/标签中以**琥珀色高亮块**标记。
  - 清空搜索：淡出的叶片重新淡入从顶部入画，恢复完整森林。
- **空结果**：舞台中央出现一片小枯叶插画 + 文案「这片林子里没找到哦，试试其他词？」。
- 命中数量 / 总数量可在底部胶囊旁的小徽标展示，或切到管理面板精确查看。

### 4.5 排序与分组（多模式切换）

底部胶囊内提供「排序」按钮，展开选项切换三种模式：

| 模式 | 说明（如何体现在飘落动画中） |
| --- | --- |
| 最新在前 | 最近修改的卡片进入**近层**（更大、更快、更清晰）；较远的进入中/远层。新卡片一保存就从近景最左/最右飘入。 |
| 随机 | 每次切换/刷新时，所有卡片随机分配层、位置、初速度。每次打开都像不同一阵秋风。 |
| 按来源分组 | 相同 `source` 的卡片：① 相同叶型主色；② 从屏幕同一水平象限下落（如 A 来源从左 1/3、B 来源从中间、C 来源从右 1/3），视觉自然分组。 |

切换时，舞台上正在飘落的叶子平滑过渡（透明度淡出 → 重新分配参数 → 淡入），不闪屏。

### 4.6 秋日氛围主题切换

从 v1 的深色主题切换改为**秋日氛围切换**（3 套），可从底部胶囊的菜单里的二级按钮切换。

| 氛围名 | 背景主色 | 线条画笔触 | 色温 |
| --- | --- | --- | --- |
| 黄昏暖金（默认） | 从 `#f6d365` 到 `#fda085` 的暖金渐变 | 棕灰 `#8b5a2b` | 暖 |
| 清晨薄雾 | 从 `#d7e4e1` 到 `#f1e1c2` 的浅青绿 + 米黄 | 灰褐 `#777766` | 微凉 |
| 雨后冷黄 | 从 `#cfd8ae` 到 `#e6c07b` 的黄绿 + 柔金 | 暗绿棕 `#556b2f` | 冷 |

- 切换后：页面背景渐变、远景线条画的笔触颜色、按钮胶囊主色、弹窗遮罩色温，整体统一变化。
- 叶片自身颜色不受氛围影响，它们是画面前景的角色。
- 氛围选择持久化存储。

---

## 5. 交互设计要点

### 5.1 底部胶囊菜单（替代顶部工具栏）
- 位置：屏幕**底部中间**，距底 24px，水平居中。
- 形态：一个大圆角胶囊（`border-radius: 999px`），米白纸面质感 + 轻微阴影，内含 4 个按钮，从左到右：
  1. 🔍 **搜索**：点击后胶囊向左展开 / 或胶囊上方弹出半透明搜索条（前者更统一，可实现为胶囊变宽）。
  2. ⇅ **排序分组**：点击弹出小气泡菜单，三个选项（最新在前 / 随机 / 按来源分组）。
  3. ☰ **管理面板**：打开管理面板抽屉 / 网格。
  4. 🍂 **+ 新建**：最大最显眼，带叶片图标；点击打开新建表单弹窗。
- 胶囊按钮用柔和的秋米白做底，hover 时轻微上抬 + 秋叶橙底色高亮。
- 胶囊左侧可以预留一个小徽标位置显示卡片总数（搜索时显示「命中 N / 总 M」）。

### 5.2 反馈
- 新建成功：Toast 文案「一片新叶已入林 🍂」，叶片从胶囊上方弧线飘入舞台。
- 编辑成功：Toast「已更新这一片」；对应叶片闪烁一下高光。
- 删除成功：Toast「叶子已飘落」；对应叶片加速下坠 + 淡出。
- 搜索有结果：Toast「找到 N 片」。
- 按钮悬停：微动效（上抬 1px + 亮度）。
- 叶片入画：淡入 + 轻微上抛后进入自然下落（模拟"被风吹起"的入画感）。

### 5.3 键盘支持
- `ESC`：关闭模态弹窗 / 退出搜索聚焦 / 关闭管理面板。
- `Ctrl/Cmd + N`：打开新建卡片表单。
- `Ctrl/Cmd + F`：聚焦搜索（胶囊自动展开搜索框）。

---

## 6. 视觉设计

### 6.1 整体风格
- **秋日插画风**：暖金 / 橙黄 / 赭石主色，浅米背景，简约线条画远景。
- **不扁平**：利用景深分层、阴影渐变、叶片纹理 / 叶脉、摇摆旋转，制造层次与呼吸感。
- **动效柔**：所有过渡走缓动曲线，速度偏慢，符合"秋日宁静"气质。

### 6.2 背景分层
1. **天空层**：径向 + 线性组合渐变，模拟秋日天空。
2. **远景线条画层**（SVG）：远处的山脉轮廓、几棵简化树、云朵、芦苇、田野地平线；全部用单线条 + 少量填充，不抢前景叶片的视觉。
3. **舞台层**（前景）：落叶飘落的整个区域。

### 6.3 配色变量（示例 · 黄昏暖金氛围）
```css
--sky-1: #f6d365;    /* 渐变上：暖金 */
--sky-2: #fda085;    /* 渐变下：橙粉 */
--line:  #8b5a2b;    /* 线条画笔触 */
--paper: #faf0d7;    /* 弹窗 / 胶囊 · 纸质底色 */
--paper-2: #f1e0b2;  /* 纸质暗面 */
--ink: #3a2817;      /* 主文字 · 墨褐 */
--ink-2: #7b5a3b;    /* 副文字 */
--accent: #d94e2f;   /* 枫叶橙红 · 高亮 / 新建按钮 */
--accent-2: #e5b93a; /* 银杏金黄 · 次高亮 */
--danger: #a9331a;   /* 删除警示 · 暗枫红 */
```

### 6.4 字体
- 系统字体栈优先（`-apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`）。
- 卡片正文行高舒适（1.7 左右），字重略偏常规，避免在叶面上过浓。
- 标题比正文大 2-3 号，字重 600-700。
- （可选）正文可使用衬线体，营造"写在落叶上的手札"质感。

### 6.5 叶片视觉要点
- 用 SVG 绘制 6 种叶型，叶脉用 `leaf.shade` 色。
- 叶片中央偏亮（用内部 radial 或叠加一层半透明白色渐变），模拟纸面。
- 每张叶片保留轻微的手绘不规则感（不要完全几何对称）。
- 叶片的阴影：`drop-shadow` 用 `rgba(80,40,10,0.25)`，与暖秋基调统一。

---

## 7. 数据存储

- **方案**：浏览器 `localStorage`，单键存储（如 `inspiration_cards`），值为卡片数组的 JSON 字符串。
  - 个人轻量工具，localStorage 足够；若后续卡片量极大可迁移 IndexedDB。
- **设置**：单独键 `inspiration_settings`，含氛围主题、排序模式、风力开关等。
- **读取时机**：页面加载时读取并生成落叶层。
- **写入时机**：每次新建 / 编辑 / 删除 / 设置变更后同步写入。
- **容错**：读取时校验 JSON 合法性与字段完整性，损坏时降级为空数组并 toast 提示「叶子林有些损伤，已修复为空林」。

---

## 8. 页面结构（信息架构）

```
┌──────────────────────────────────────────────────────┐
│                    秋日天空背景 + 远山线画             │
│  🍂      🍁       🍂      🍃        🍁        🍂    │
│        （叶片从上方飘落，近中远三层，摇摆 + 旋转）     │
│                                                        │
│                  🍁                                    │
│            🍂            🍃                            │
│                                                        │
│                              🍁                         │
│    🍃              🍂                                  │
│                                                        │
│                    地平线 / 芦苇 / 远树 线画           │
├──────────────────────────────────────────────────────┤
│           [卡片总数徽标]                                │
│            ┌──┬──┬──┬────┐                             │
│            │🔍│⇅ │☰ │🍂 +│ ← 胶囊菜单（底部中央）      │
│            └──┴──┴──┴────┘                             │
└──────────────────────────────────────────────────────┘

叠加层：
- 详情模态弹窗（居中 · 叶片放大形态 + 遮罩）
- 新建/编辑模态弹窗（居中 · 纸质卡片 + 遮罩）
- 管理面板（右侧滑入抽屉 · 网格视图）
- 排序/氛围 小气泡菜单（从胶囊按钮弹出）
- 删除二次确认弹窗 / Toast
```

---

## 9. 技术约束

- **纯前端三件套**：HTML + CSS + JavaScript，单页应用，无构建依赖。
- 拆分为 `index.html` / `style.css` / `app.js`（必要时叶片 SVG 可内嵌 HTML 或用模板绘制）。
- 不依赖后端服务、不联网；字体可用系统字栈。
- 数据仅存本地浏览器，不跨设备同步。
- 动画用 `requestAnimationFrame` 实现（sin 摇摆、风力、分层），性能上控制每层同时显示的叶片数量（可做虚拟化 / 池化，避免上百卡时掉帧）。
- 裁剪使用 `clip-path` + SVG path；兼容回退为近似多边形蒙版。
- 兼容主流现代浏览器（Chrome、Edge、Firefox、Safari 最新版）。

---

## 10. 已确认的产品决策（v2）

| 决策项 | 选择 |
| --- | --- |
| 主题氛围 | 秋风落叶 · 秋日金黄主色 + 简约秋日线条画 |
| 卡片外观 | **叶片就是卡片**：SVG 叶型裁剪 + 6 种叶型（枫/银杏/梧桐/橡叶/柳/桦） |
| 叶型映射 | **叶型 ↔ 标签**自动分配（6 类映射表）；可手动修改 |
| 展示形态 | **近/中/远三层循环飘落**，从上到下；sin 摇摆 + 轻微旋转 + 全局风力 |
| 卡片字段 | 完全保留原结构（标题/正文/来源/标签/时间），新增 `leaf` 字段，移除 `bg` |
| 悬停行为 | 该叶片**冻结下落 + 放大 + 浮现操作按钮**；移出后 1 秒恢复 |
| 菜单布局 | **屏幕底部中央的胶囊形按钮组**（4 个：搜索 / 排序 / 管理 / 新建） |
| 搜索表现 | 输入关键词 → **未命中叶片淡出消失**，命中的继续飘落并关键词高亮 |
| 保留功能 | CRUD + 删除二次确认、详情弹窗、排序分组三模式、独立管理面板 |
| 氛围切换 | 3 套秋日氛围：黄昏暖金（默认）/ 清晨薄雾 / 雨后冷黄 |
| 排序飘落化 | 最新=进入近层，随机=重分配参数，按来源=分区域下落 + 同叶主色 |

---

## 11. 后续可扩展（暂不实现）

- 导入/导出 JSON 备份
- 更丰富的叶型 / 自定义 SVG 上传
- 背景线画季节切换（春 / 夏 / 秋 / 冬）
- 秋风吹动音效（极轻音量，可开关）
- 卡片分享（生成一张"落叶明信片"图片）
- 「收集」交互：把飘落的叶片拖到篮子里打标 / 归档
