# 灵感卡片墙 · 秋风落叶 主题设计规范

> 版本：v1.0 | 更新日期：2026-08-18 | 适用项目：纯前端 HTML/CSS/JS 单页应用
> 设计哲学：**让信息以落叶的姿态自然飘落，玻璃质感与秋日金黄的温暖相遇。**

---

## 目录

1. [设计理念](#1-设计理念)
2. [色彩系统（Design Tokens）](#2-色彩系统design-tokens)
3. [排版规范](#3-排版规范)
4. [尺寸与间距](#4-尺寸与间距)
5. [玻璃材质规范（Glassmorphism）](#5-玻璃材质规范glassmorphism)
6. [组件设计](#6-组件设计)
7. [落叶 SVG 素材库](#7-落叶-svg-素材库)
8. [物理动画系统](#8-物理动画系统)
9. [交互状态机](#9-交互状态机)
10. [响应式适配](#10-响应式适配)
11. [背景层 SVG 素材](#11-背景层-svg-素材)
12. [动效编排节奏表](#12-动效编排节奏表)
13. [复现检查清单](#13-复现检查清单)

---

## 1. 设计理念

### 1.1 核心隐喻

**"秋风中飘落的灵感"** —— 每一张卡片是一片秋叶，在秋意天空中缓缓飘落。用户捕捉它（悬停）、拾起它（点击展开）、珍藏它（保存）、让它飘走（删除）。

### 1.2 视觉关键词

| 维度 | 关键词 |
|------|--------|
| **色调** | 金秋暖调 / 真实秋叶色 / 低饱和不刺眼 |
| **材质** | 毛玻璃 / 有机自然 / 柔焦景深 |
| **动效** | 自然物理 / 呼吸感 / 平滑过渡 / 无突兀跳变 |
| **层次** | 三层景深（远景/中景/近景）/ z-index 严格分层 |
| **交互** | MAC Dock 磁吸 / 软约束而非硬重置 / 轻触即懂 |

### 1.3 严格约束

- ❌ **禁止硬重置**：新增/编辑/删除后，其余叶子必须继续自由下落，绝不整体重建
- ❌ **禁止突兀推力**：仅左右边框有斥力，上边框绝对不加
- ✅ **二次确认删除**：先播放飘走动画，再真正删除
- ✅ **数据持久化**：所有数据存 localStorage

---

## 2. 色彩系统（Design Tokens）

所有颜色通过 CSS 自定义属性（CSS Variables）管理，挂在 `body[data-atmosphere="xxx"]` 上。

### 2.1 全局不变量（:root）

```css
:root {
  /* 叶片尺寸 */
  --leaf-w: 300px;
  --leaf-h: 360px;          /* 比例 5:6 */

  /* 圆角 */
  --radius: 16px;
  --radius-sm: 10px;

  /* 阴影 */
  --shadow-soft: 0 8px 28px rgba(80, 40, 10, 0.18);
  --shadow-deep: 0 18px 48px rgba(80, 40, 10, 0.28);

  /* 叶柄（真实棕色，绝对不染成叶子色） */
  --stem-color: #5d3a1a;
}
```

### 2.2 三套氛围主题

#### 🌇 黄昏暖金（默认，`data-atmosphere="dusk"`）

```css
/* 天空渐变（上→中→下） */
--sky-1: #f6d365;   /* 顶部金 */
--sky-2: #fda085;   /* 底部霞 */
--sky-3: #f8b97a;   /* 中接色 */

/* 线条画（远山/树/芦苇） */
--line:      #8b5a2b;                  /* 主描线：棕 */
--line-soft: rgba(139, 90, 43, 0.45);  /* 副描线：半透明 */

/* 纸张（弹窗/管理面板底色） */
--paper:        #faf0d7;                 /* 米黄主纸 */
--paper-2:      #f1e0b2;                 /* 稍深次级纸 */
--paper-border: rgba(139, 90, 43, 0.22); /* 纸边描边 */

/* 文字墨水（三级层次） */
--ink:   #3a2817;   /* 主文字：深棕 */
--ink-2: #7b5a3b;   /* 副文字：中棕 */
--ink-3: #a08566;   /* 辅助文字：浅棕 */

/* 强调色 */
--accent:   #d94e2f;   /* 主强调：枫叶红 */
--accent-2: #e5b93a;   /* 副强调：银杏金 */
--danger:   #a9331a;   /* 危险：暗红 */

/* 遮罩 */
--overlay: rgba(120, 70, 30, 0.45);

/* 玻璃材质 */
--glass-bg:     rgba(250, 240, 215, 0.35);
--glass-border: rgba(255, 255, 255, 0.30);
```

#### 🌫️ 清晨薄雾（`data-atmosphere="mist"`）

```css
--sky-1: #d7e4e1; --sky-2: #f1e1c2; --sky-3: #e6ead0;
--line:  #777766; --line-soft: rgba(119,119,102,0.40);
--paper: #f5f0e8; --paper-2: #e6e0d2; --paper-border: rgba(119,119,102,0.22);
--ink:   #4a4030; --ink-2: #7a705c; --ink-3: #9a9080;
--accent:#c77b34; --accent-2: #b98832; --danger: #a05a2c;
--overlay: rgba(90, 90, 80, 0.40);
--glass-bg: rgba(245,240,232,0.35); --glass-border: rgba(255,255,255,0.28);
```

#### 🌧️ 雨后冷黄（`data-atmosphere="rain"`）

```css
--sky-1: #cfd8ae; --sky-2: #e6c07b; --sky-3: #d8d09a;
--line:  #556b2f; --line-soft: rgba(85,107,47,0.42);
--paper: #ecefe0; --paper-2: #dde0cc; --paper-border: rgba(85,107,47,0.24);
--ink:   #3a3a17; --ink-2: #6b6b3a; --ink-3: #8a8a5a;
--accent:#a9532a; --accent-2: #8a8a2a; --danger: #7a3a1a;
--overlay: rgba(60, 70, 30, 0.42);
--glass-bg: rgba(236,239,224,0.35); --glass-border: rgba(255,255,255,0.25);
```

### 2.3 真实秋色调色板（12 色预设）

按色相顺时针分布：**深红 → 橙 → 金黄 → 暗棕**

```
#b21f0e  红枫深
#c1351a  红枫中
#d94e2f  红枫亮    ← 语录类默认
#e07a2c  橙红
#e88c30  橙
#e8a823  金        ← 读书类默认
#f0c041  金黄
#d9a441  土黄
#c9742a  橙棕      ← 灵感类默认
#a8782c  黄褐      ← 写作类默认
#8f3f22  暗红棕    ← 摘抄类默认
#6b2e15  褐黑
```

> **调色原则**：不使用纯黄（#ffff00 类刺眼色），所有颜色的饱和度 ≤ 75%，亮度在 35%–90% 之间。每一片叶子的颜色都能在真实秋天找到对映。

### 2.4 标签 → 叶型/色 映射表

| 标签匹配 | 叶型 | 颜色 |
|---------|------|------|
| 语录 | maple 枫 | `#c1351a` 深红枫 |
| 读书 / 读书笔记 / 阅读 | ginkgo 银杏 | `#e8a823` 金黄 |
| 灵感 | platanus 梧桐 | `#c9742a` 橙棕 |
| 写作 | birch 桦 | `#a8782c` 黄褐 |
| 摘抄 / 摘录 | oak 橡 | `#8f3f22` 暗红棕 |
| 未命中 | birch 桦 | `#d9a441` 土黄 |

---

## 3. 排版规范

### 3.1 字体栈

```css
font-family:
  -apple-system, BlinkMacSystemFont, "Segoe UI",
  "PingFang SC", "Microsoft YaHei", "Helvetica Neue",
  "Songti SC", serif, Arial, sans-serif;
```

> **选择理由**：苹方/微软雅黑保证中文清晰，衬线字体（Songti SC）呼应"古典纸质"感，最后用 serif 兜底保证长文阅读舒适。

### 3.2 字号层级（按组件）

#### 落叶上的文字（居中白字 + 黑阴影）
| 元素 | 字号 | 字重 | 行高 | 截断 |
|------|------|------|------|------|
| `.leaf-title` | 24px | **800** (ExtraBold) | 1.25 | `-webkit-line-clamp: 2`（2行截断） |
| `.leaf-body`  | 15px | 400 | 1.55 | `-webkit-line-clamp: 4`（4行截断） |
| 文字阴影 | `0 1px 3px rgba(0,0,0,0.35)` | — | — | — |

> **绝对约束**：文字区域（`.leaf-content`）必须完全在叶片轮廓内，具体定位：
> ```css
> top: 25%; bottom: 28%; left: 22%; right: 22%;
> ```
> 对应 300×360 叶子 → 文字实际占用约 **168×169px**（预留叶柄和叶尖的不可用区域）

#### 弹窗文字
| 元素 | 字号 | 字重 |
|------|------|------|
| `.detail-title` | 26px | 700 |
| `.detail-body` | 16px | 400 |
| `.modal-heading` | 22px | 700 |
| `.field-label` | 13px | 600 |
| `.detail-tag` | 12px | 400 |
| `.detail-info` | 13px | 400 |

#### 管理面板卡片（缩略）
| 元素 | 字号 | 字重 |
|------|------|------|
| `.m-title` | 14px | 700 |
| `.m-body`  | 12px | 400（2行截断） |
| `.m-meta`  | 10px | 400 |
| `.m-tag`   | 9px  | 400 |

### 3.3 选中文本高亮

```css
::selection { background: var(--accent-2); color: var(--ink); }
```

---

## 4. 尺寸与间距

### 4.1 栅格基准：8px

所有内边距/外边距遵循 **8 的倍数**（允许 4px 微调）。

```
4px   · 标签 chip 内边距 / Dock label 内边距
8px   · 次级间距
12px  · 常用间距
16px  · 卡片圆角 / 输入框内边距
20px  · 大间距
24px  · 弹窗内边距
32px  · 巨间距
```

### 4.2 叶片尺寸比例

叶片是整个视觉的核心，必须严格保持 **5 : 6** 比例（宽 : 高）：

| 断点 | 宽 | 高 | 文字区 |
|------|-----|-----|--------|
| ≥ 721px（默认） | 300px | 360px | 24/15px |
| ≤ 720px（移动端） | 240px | 288px | 20/13px |

### 4.3 三层景深层参数（LAYERS）

这是实现景深的关键，**不要修改相对比例**：

```js
[
  { name: 'far',  scale: 0.65, opacity: 0.55, speed: 0.18, swayAmp: 12, z: 1 },  // 远景：小、半透明、慢
  { name: 'mid',  scale: 0.85, opacity: 0.82, speed: 0.32, swayAmp: 28, z: 2 },  // 中景：中间
  { name: 'near', scale: 1.05, opacity: 1.00, speed: 0.50, swayAmp: 46, z: 3 },  // 近景：大、快、实
]
```

- `scale` 控制 CSS transform: scale()
- `opacity` 控制基础透明度（模拟大气透视）
- `speed` 是 px/帧（≈ 60fps），**近快远慢**制造视差
- `swayAmp` 横向摆动像素幅度，**近景摆动幅度更大**
- `z` 是 z-index 层序，**远景永远在近景之下**

---

## 5. 玻璃材质规范（Glassmorphism）

### 5.1 三级玻璃层次（从实到虚）

| 级别 | 适用 | backdrop-filter | 背景色透明度 | 边框透明度 |
|------|------|-----------------|-------------|-----------|
| **T1（强玻璃）** | Dock 主面板 / 展开搜索结果 | `blur(28px) saturate(180%)` | 35% | 30% |
| **T2（中玻璃）** | 顶部徽标 / Toast / 弹窗遮罩 | `blur(16px)` | 35% | 28% |
| **T3（弱玻璃）** | 排序/氛围气泡 | `blur(20px)` | 35% | 25% |
| **T4（微玻璃）** | 操作按钮（编辑/删除） | `blur(6px)` | 40%（黑遮罩） | 40%（白边） |

### 5.2 Dock 玻璃面板完整参数（核心！）

```css
.dock-panel {
  padding: 10px 14px;
  border-radius: 24px;                              /* 胶囊圆角 */
  background: rgba(250, 240, 215, 0.35);            /* 纸色 35% */
  backdrop-filter: blur(28px) saturate(180%);       /* 强玻璃 + 饱和提升 */
  -webkit-backdrop-filter: blur(28px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.30);      /* 顶光：白边 */
  /* 三层阴影结构：外投影 + 内高光 + 内暗边 */
  box-shadow:
    0 12px 40px rgba(80, 40, 10, 0.18),             /* 外投阴影 */
    inset 0 1px 1px rgba(255, 255, 255, 0.5),       /* 内高光（顶） */
    inset 0 -1px 1px rgba(80, 40, 10, 0.05);        /* 内暗边（底，立体感） */
  transition: background 0.8s ease;                  /* 切换氛围时玻璃色平滑过渡 */
}
```

### 5.3 玻璃材质的关键规则

1. **必须同时设置** `background`（半透明底色）**和** `backdrop-filter: blur()`
2. **必须加边框**：1px 白色半透明（`rgba(255,255,255,0.25~0.3)`），这是"玻璃高光边"的关键
3. **inset 双层阴影**：顶部 `inset 0 1px 1px rgba(255,255,255,0.5)` + 底部微暗边，制造玻璃曲面的高光
4. **背景色不是白色**：玻璃底色 = `--paper` 米黄 × 35%，和整体暖调一致
5. **saturate(180%)**：让模糊后的背景色彩不灰闷

---

## 6. 组件设计

### 6.1 底部 Dock（MAC 风格磁吸推开）

#### 结构

```
.dock-outer (fixed bottom 18px center)
└── .dock-panel (glass capsule, flex align-items: flex-end)
    ├── .dock-search (input, hidden by default)
    ├── .dock-item × 4（搜索/排序/氛围/管理，50×50）
    └── .dock-item-primary（新建，56×56，渐变色）
```

#### 视觉参数

```css
.dock-item {
  width: 50px; height: 50px;
  border-radius: 14px;
  background: var(--glass-bg);
  backdrop-filter: blur(8px);
  border: 1px solid var(--glass-border);
  color: var(--ink-2);
  /* 关键：用 width/height transition 而非 scale，让 flexbox 自动让位！ */
  transition:
    width  0.18s cubic-bezier(.2,.7,.3,1.2),
    height 0.18s cubic-bezier(.2,.7,.3,1.2),
    background 0.2s, color 0.2s, box-shadow 0.2s;
  flex-shrink: 0;   /* 绝不被压缩 */
}
.dock-item:hover, .dock-item:focus-visible {
  width: 80px; height: 80px;            /* MAC 磁吸：放大 1.6x */
  background: var(--accent);
  color: #fff;
  box-shadow: 0 6px 18px color-mix(in srgb, var(--accent) 40%, transparent);
}
.dock-item-primary {
  width: 56px; height: 56px;           /* 主按钮平时稍大 */
  background: linear-gradient(135deg, var(--accent), var(--accent-2));
  color: #fff;
}
.dock-item-primary:hover {
  width: 88px; height: 88px;           /* 主按钮放大到 1.57x */
  filter: brightness(1.1);
}
```

> **⚠️ 磁吸推开的核心实现**：**不能**用 `transform: scale()`，因为 scale 不影响布局流，相邻控件会重叠。必须用 **width/height 变化** + `display: flex align-items: flex-end gap: 12px`，让 flexbox 自然调整间距。

#### MAC 磁吸公式（JS 实时计算）

```js
// 计算鼠标在控件上的"距离影响力"：类似高斯分布
function dockMagnetic(mouseX, itemCenterX, itemSize) {
  const dist = Math.abs(mouseX - itemCenterX);
  const sigma = itemSize * 2.2;   // 影响半径
  const influence = Math.exp(-(dist * dist) / (sigma * sigma));
  // 放大 = 基础尺寸 + influence * (最大放大 - 基础尺寸)
  return baseSize + influence * (maxSize - baseSize);
}
// 相邻控件会因为中间控件变宽而被自然推到两侧（flex gap 自动调整）
```

#### Dock label 气泡

```
hover 时出现
.dock-label
  position: absolute; bottom: calc(100% + 10px);
  background: var(--paper);
  border-radius: 8px;
  font-size: 12px;
  opacity: 0 → 1（0.2s）
  transform: translateX(-50%) translateY(0)
  ← 从 translateY(2px) 过渡上来
```

#### 排序/氛围气泡

```
.dock-pop
  position: absolute; bottom: calc(100% + 14px);
  background: var(--glass-bg); backdrop-filter: blur(20px);
  border-radius: 14px;
  min-width: 170px;
  animation: popIn 0.2s cubic-bezier(.2,.7,.3,1.2);
    ↑ 从 translate(8px 下, 0 透明) 弹入

按钮 hover → background: var(--paper-2)
按钮 active → color: var(--accent); font-weight: 600
```

#### 搜索条展开

```
.dock-search
  默认 hidden
  展开时 animation: searchExpand 0.3s cubic-bezier(.2,.7,.3,1.2);
    从 width:0 opacity:0 → width:220px opacity:1
  input width: 180px; font-size: 14px
```

### 6.2 单片落叶（核心交互单元）

#### DOM 结构

```html
<div class="leaf" data-id="xxx">
  <svg class="leaf-shape" viewBox="0 0 100 120">
    <path class="leaf-stem" d="M50,78 L50,112"/>   <!-- 叶柄：染 stem-color -->
    <path class="leaf-body-path" d="...叶身轮廓..." fill="叶色"/>
    <path class="leaf-vein-line" d="M50,76 L50,15"/>  <!-- 叶脉：浅色半透 -->
    ...
  </svg>
  <div class="leaf-content">
    <h3 class="leaf-title">标题</h3>
    <p class="leaf-body">正文（截断）</p>
  </div>
  <div class="leaf-actions">
    <button data-act="edit">✎</button>
    <button data-act="delete">🗑</button>
  </div>
</div>
```

#### 三层 SVG 严格分工（叶柄不染成叶身色！）

| class | 填充/描边 | 说明 |
|-------|----------|------|
| `.leaf-stem` | `fill: var(--stem-color) = #5d3a1a` | **固定棕色**，绝对不随叶色变化 |
| `.leaf-body-path` | `fill: 叶色（12预设）` `stroke: rgba(0,0,0,0.18) stroke-width:0.6` | 叶身轮廓 + 细描边 |
| `.leaf-vein-line` | `stroke: rgba(255,240,200,0.5) stroke-width:1.1 fill:none` | 浅色叶脉，pointer-events 穿透 |

#### 落叶滤镜（投影）

```css
filter: drop-shadow(0 6px 12px rgba(80, 40, 10, 0.22));
```

> 用 `drop-shadow` 而非 `box-shadow`：前者跟随 SVG 叶子形状投影，后者是矩形投影。

#### 悬停交互流程（关键动效编排）

```
鼠标进入 (mouseenter):
  ① 立即 frozen=true → 停住（speed 不再累加）
  ② lerp 在 ~200ms 内完成：
     - rot → 0°（摆正）
     - scale × 1.18（轻微放大，HOVER_SCALE）
     - z-index → 9999（移到最前层）
  ③ 显示 .leaf-actions（编辑/删除按钮，淡入 + 上移 4px）

鼠标离开 (mouseleave):
  ① frozen=false
  ② z-index 恢复 baseZ（1/2/3）
  ③ scale/rot 不再锁定，自然回到下落动画
  ④ .leaf-actions 淡出
```

**lerp 公式**（每帧在 requestAnimationFrame 中调用）：
```js
const LERP_K = 0.18;   // 每帧插值系数：越大越"快"
m.rot      = lerp(m.rot,      m.frozen ? 0 : m.rotSway,    LERP_K);
m.curScale = lerp(m.curScale, m.frozen ? m.scale*1.18 : m.scale, LERP_K);
```

#### 点击展开全屏（核心交互！）

```
click 叶子:
  ① 当前叶 frozen=true 并记录 expandingMotion = m
  ② 计算目标 transform:
     x = vw/2 - leafW/2
     y = vh/2 - leafH/2
     rot = 0
     scale = max(vw / leafW, vh / leafH) * 1.3   (覆盖整个屏幕 + 余量)
  ③ leaf.classList.add('expanding'):
     - z-index: 20000 !important
     - will-change: auto
     - filter: none !important (移除投影，因为全屏不需要)
  ④ 用 transition 200ms 过渡到目标（cubic-bezier(.2,.7,.3,1.2)）
  ⑤ 720ms 后展开详情弹窗 overlay（fadeIn 0.25s + backdrop-filter: blur(8px)）
  ⑥ 关闭详情时，叶子平滑回到原位，移除 expanding class
```

> **⚠️ 常见坑**：不能用 `transform-origin: 50% 50%` 计算位置，因为 transform 的 translate 和 scale 会相互影响。**正确做法**：始终以左上角为原点（`transform-origin: 0 0`），手动计算 `x = vw/2 - leafW*scale/2` 和对应 `y`。

#### 删除飘走动画

```
confirm delete:
  ① flyAway=true; frozen=true;
  ② 每帧：
     x += flyVx * dt                 (2~5 px/帧 向右)
     y += flyVy * dt                 (-3~-7 px/帧 向上)
     flyVy += 0.15 * dt              (微弱重力，先升后自然飘)
     rot += flyRot * dt              (3~8 度/帧 持续旋转)
     opacity -= 0.018 * dt           (1.2秒内完全淡出)
     z-index: 9999                   (始终在最上层飘走)
  ③ 1.2秒后：
     - 从 localStorage 删除数据
     - 从 motions 数组移除该叶子
     - 从 DOM 移除该叶子（只移除该叶，不重建其他！）
```

### 6.3 操作按钮（编辑/删除）

```css
.leaf-actions button {
  width: 36px; height: 36px;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.4);
  backdrop-filter: blur(6px);              /* 微玻璃 */
  border: 1px solid rgba(255,255,255,0.4); /* 白边 */
  color: #fff;
  transition: background 0.2s, transform 0.15s;
}
:hover → background: rgba(0,0,0,0.7); transform: scale(1.12);
```

### 6.4 弹窗（Overlay + Modal）

```
.overlay
  position: fixed; inset: 0;
  z-index: 20000;                       (必须大于 expanding 叶子的 z=20000? → 实际 20000 相同但 overlay 后渲染，会盖住)
  background: var(--overlay)
  backdrop-filter: blur(8px);
  animation: fadeIn 0.25s ease;

.modal
  background: var(--paper);
  border: 1px solid var(--paper-border);
  border-radius: var(--radius);
  box-shadow: var(--shadow-deep);
  animation: popIn 0.3s cubic-bezier(.2,.7,.3,1.2);
    ← 从中心弹入（比 fadeIn 更有弹性）
  transition: background 0.8s ease;     (氛围切换时同步变色)

  .close-btn (top-right):
    32×32; border-radius 50%
    background: var(--paper-2)
    :hover → var(--accent) + color white + rotate(90deg)
```

#### 详情弹窗（detail-modal）

```
尺寸: 620px / 92vw; max-height: 86vh; padding: 36px 32px 24px
结构:
  .detail-leaf-corner (左上角挂一片小叶子 50×50，和被点击的叶子同型同色)
  .detail-title (26px 700)
  .detail-body (16px 1.8行高 white-space: pre-wrap)
  .detail-meta:
    .detail-tags (chip wrap)
    .detail-info: 来源 / 时间
  .modal-actions: ghost-btn(编辑) / danger-btn(删除)
```

#### 编辑弹窗（edit-modal）

```
尺寸: 620px / 94vw; max-height: 90vh; padding: 30px
表单字段:
  标题 *  (input, maxlength 60)
  正文 *  (textarea, min-height 100px, resize vertical)
  来源    (input, maxlength 80)
  标签    (tags-box 已添加 chip + tagInput 回车添加)
  叶片外观:
    叶型选择（leaf-type-btn: 56×56, active 时 border: var(--accent)）
    颜色预设（preset-color: 28px 圆形，选中 border: --ink）
    自定义 color picker
    自动选叶型 checkbox (按标签映射)
  预览    (preview-leaf 300×360, 实时显示外观)
  操作    : ghost-btn(取消) / primary-btn(让这片叶飘起)
```

#### 删除确认弹窗（confirm-modal）

```
尺寸: 360px / 90vw; padding: 30px 28px 24px; 居中
结构:
  .confirm-leaf (🍂, 44px, leafFloat 动画 2.4s 循环)
  "确认让这片叶子飘走？" (17px 600)
  "此操作不可撤销" (13px, --ink-3)
  操作: ghost-btn(留下它) / danger-btn(让它飘走)
```

### 6.5 管理面板（侧边抽屉）

```
.manage-panel
  position: fixed; top:0 right:0 bottom:0
  width: 580px / 92vw
  z-index: 90
  background: var(--paper)
  border-left: 1px solid --paper-border
  box-shadow: -20px 0 60px rgba(80,40,10,0.3)
  animation: slideIn 0.3s cubic-bezier(.2,.7,.3,1.2);
    ← 从右 100% → 0 滑入

  .manage-head:
    h2 18px 700 "落叶归档"
    search input (999px, focus border: --accent)
    close-btn

  .manage-grid:
    overflow-y: auto
    padding: 18px 20px
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr))
    gap: 14px

    .m-card:
      border-radius: --radius-sm
      background: --paper-2 / hover: --paper
      padding: 14px / gap: 12px / display: flex
      :hover → transform: translateY(-2px)

      .m-leaf (52×62, 小叶子缩略)
      .m-info:
        .m-title 14px 700
        .m-body 12px (2行截断)
        .m-meta: 来源 / 时间
        .m-tags (9px chip)
        .m-actions: 编辑 / 删除（删除 hover 红色）
```

### 6.6 Toast 提示

```
.toast
  position: fixed; bottom: 100px (在 Dock 之上); left: 50%;
  z-index: 200
  padding: 12px 22px; border-radius: 999px
  background: var(--glass-bg); backdrop-filter: blur(16px);
  border: 1px solid --glass-border
  animation: toastIn 0.3s cubic-bezier(.2,.7,.3,1.2);
    ← 从 translate(0, 14px) + transparent 弹入
```

### 6.7 顶部徽标

```
.top-badge
  fixed top: 18px left: 18px; z-index: 20
  padding: 7px 14px; border-radius: 999px
  background: --glass-bg; backdrop-filter: blur(16px);
  border: 1px solid --glass-border
  font-size: 13px; color: --ink-2
  内容: 🍂 + "X 片"
```

### 6.8 空状态

```
.empty-state
  绝对定位全屏居中
  🍂 56px（leafFloat 3s 循环浮动动画）
  "这片林子还空着" (20px 700)
  "点底部的「+ 新建」让第一片叶子飘进来" (14px --ink-3)
```

---

## 7. 落叶 SVG 素材库

### 7.1 viewBox 规范

所有叶子 SVG 的 **viewBox 必须统一为 `0 0 100 120`**（宽 100 : 高 120 = 5:6，匹配 CSS 的 300:360）。

### 7.2 五种叶型定义（已移除瘦长柳叶）

#### 🍁 枫叶 maple（五裂经典枫叶）

```
叶身: M50,10 L58,28 L70,22 L66,38 L82,42 L72,54 L84,72 L62,64 L60,86 L50,78 L40,86 L38,64 L16,72 L28,54 L18,42 L34,38 L30,22 L42,28 Z
叶柄: M50,78 L50,112
叶脉: 主脉+左右侧脉共7条 [M50,76→50,15 / M50,42→24,28 / M50,42→76,28 / M50,55→20,55 / M50,55→80,55 / M50,66→42,80 / M50,66→58,80]
```

#### 🍂 银杏 ginkgo（扇形顶部微凹）

```
叶身: M50,88 Q20,78 22,42 Q26,14 45,14 Q50,24 55,14 Q74,14 78,42 Q80,78 50,88 Z
叶柄: M50,88 L50,114
叶脉: 6条放射扇形脉 [M50,86→30,22 / M50,86→40,18 / M50,86→48,16 / M50,86→52,16 / M50,86→60,18 / M50,86→70,22]
```

#### 🌿 梧桐 platanus（掌状3~5裂）

```
叶身: M50,12 Q35,16 30,30 Q15,28 18,48 Q12,58 25,66 Q22,80 38,80 Q42,92 50,86 Q58,92 62,80 Q78,80 75,66 Q88,58 82,48 Q85,28 70,30 Q65,16 50,12 Z
叶柄: M50,86 L50,112
叶脉: 5条 [M50,84→50,18 / M50,45→22,36 / M50,45→78,36 / M50,60→20,62 / M50,60→80,62]
```

#### 🟫 橡树 oak（波浪边）

```
叶身: M50,10 Q40,12 35,22 Q28,20 25,30 Q18,32 22,42 Q15,48 22,55 Q15,62 25,65 Q22,75 32,75 Q35,85 42,82 Q46,90 50,88 Q54,90 58,82 Q65,85 68,75 Q78,75 75,65 Q85,62 78,55 Q85,48 78,42 Q82,32 75,30 Q72,20 65,22 Q60,12 50,10 Z
叶柄: M50,88 L50,112
叶脉: 7条 [M50,86→50,18 / M50,35→25,32 / M50,35→75,32 / M50,50→22,50 / M50,50→78,50 / M50,65→25,68 / M50,65→75,68]
```

#### 🟨 桦树 birch（卵形圆润）

```
叶身: M50,12 Q68,15 72,35 Q75,58 65,75 Q58,88 50,88 Q42,88 35,75 Q25,58 28,35 Q32,15 50,12 Z
叶柄: M50,88 L50,112
叶脉: 9条 [M50,86→50,18 / M50,30→35,28 / M50,30→65,28 / M50,45→30,48 / M50,45→70,48 / M50,60→32,65 / M50,60→68,65 / M50,75→40,80 / M50,75→60,80]
```

### 7.3 SVG 生成函数（参考）

```js
function leafSvgInner(type, color) {
  const def = LEAF_DEFS[type];
  const veins = def.veins.map(v =>
    `<path class="leaf-vein-line" d="${v}"/>`).join('');
  return `
    <path class="leaf-stem" d="${def.stem}"/>
    <path class="leaf-body-path" fill="${color}" d="${def.body}"/>
    ${veins}
  `;
}
```

> **⚠️ 常见坑**：叶柄 `.leaf-stem` 的 fill 必须是 CSS 变量 `--stem-color`，不能用 JS 填色。叶身 `.leaf-body-path` 的 fill 才是叶子自定义色。叶脉用固定浅金色半透明，不染。

### 7.4 不使用的叶型（已移除）

- ❌ 柳叶 willow：过于瘦长，矩形文字区极易超出叶片轮廓

---

## 8. 物理动画系统

### 8.1 动画循环骨架（requestAnimationFrame）

```js
function tick(time) {
  const dt = Math.min(48, time - lastTime);  // 限制单帧最大 dt，避免切回后大跳
  lastTime = time;

  wind += (windTarget - wind) * 0.008;       // 风向缓动
  if (需要换风向) windTarget = rand(-0.8, 0.8);

  计算斥力();   // 先算出所有叶子 fx/fy
  for (const m of motions) 更新叶子(m, dt);  // 再更新位置

  rafId = requestAnimationFrame(tick);
}
```

### 8.2 单叶下落物理（正常状态）

```js
// 摆动
const sway = Math.sin(time/1000 * (2*PI/m.swayPeriod) + m.swayPhase) * m.swayAmp;
// 旋转
const rotSway = m.rotStart + Math.sin(time/1000 * (2*PI/m.rotPeriod) + m.rotPhase) * m.rotAmp;
// 风力位移
const windX  = wind * (2 + m.layerIdx) * 4;
// 累计（每帧 dt 调整，假设 60fps = dt=16.67ms）
const frameRatio = dt / 16.67;
m.y += m.speed * frameRatio;                    // 下落
m.x += sway      * 0.004 * frameRatio;          // 摆动
m.x += windX     * frameRatio;                  // 风力
// 透明度（顶部缓入）
m.curOpacity = easeOutCubic(1 - m.fadeInT/600) * m.baseOpacity;
```

### 8.3 软斥力系统（关键：软约束不硬重置）

#### (1) 屏幕左右边缘斥力

```
参数:
  edgeMargin = 90px          （距边缘 < 90px 才开始推）
  edgeK      = 0.35
实现:
  if (叶左边界 < 90px):
    d = 90 - 叶左边界
    fx += 0.35 * d² * 0.005 + 0.6    （+：向右推）
  if (叶右边界 > vw - 90px):
    d = 叶右边界 - (vw - 90)
    fx -= 0.35 * d² * 0.005 + 0.6    （-：向左推）
```

> **⚠️ 严格禁止上边框斥力**：用户明确要求，避免叶子突然被向下推的突兀感。叶子从顶部自然进入即可。

#### (2) 叶片之间斥力

```
参数:
  pairMinDist = 165px × 两叶平均scale    （两中心距小于此值才推开）
  pairK       = 1.4
实现 (两两配对 O(n²)，叶子少时可接受):
  对每对 (a, b):
    dist = sqrt((ax-bx)² + (ay-by)²)
    if (dist < pairMinDist):
      overlap = pairMinDist - dist
      nx = (bx-ax)/dist  (单位向量)
      ny = (by-ay)/dist
      f = pairK * overlap² * 0.01
      a.fx -= nx * f;    a.fy -= ny * f * 0.7
      b.fx += nx * f;    b.fy += ny * f * 0.7
      ↑ y 方向只推 70%，避免干扰下落主旋律
```

#### (3) 斥力的适用范围（状态过滤）

以下情况**绝对不加斥力**（避免和自身动画冲突）：

| 状态 | 原因 |
|------|------|
| `expanding`（正在全屏展开） | 有自己的 transition 定位 |
| `flyAway`（正在删除飘走） | 有自己的飞离轨迹 |
| `frozen && hover`（正在悬停） | 被用户捕获，不应被推开 |
| `searchMatched && hexTarget`（飞向六角堆积） | 有自己的堆积目标 lerp |

### 8.4 顶部缓入（fade-in）

```
持续时间: 600ms
缓动函数: easeOutCubic(t) = 1 - (1-t)³
起始: y < 0（屏幕外上方） + fadeInT = 600 + curOpacity = 0
每帧: fadeInT = max(0, fadeInT - dt)
      curOpacity = easeOutCubic(1 - fadeInT/600) × baseOpacity
结束: fadeInT = 0, curOpacity = baseOpacity
```

### 8.5 释放节奏调度（避免集中）

#### 初始化时错开（renderStage）

```
估算单片叶子下落时长:
  fallPx = vh + 340       （屏幕高 + 叶子高 + 缓冲）
  fallMs = (fallPx / speed) × 16.67
第 i 片叶子的延迟:
  releaseDelay = (i / n) × fallMs × 0.7
动画循环处理:
  if (releaseDelay > 0):
    releaseDelay -= dt
    el.style.opacity = 0  ← 延迟期间完全隐藏
    return                ← 不下落、不摆动、不计算斥力
```

#### 回收后重新进入错开

```
叶子飘出屏幕底后重新回到顶部时:
  interval = max(400ms, min(2500ms, 8000ms / 可见叶子数))
  releaseDelay = rand(interval × 0.3, interval)
```

### 8.6 搜索过渡动画

#### 未命中叶子（加速下落 + 淡出）

```
searchHidden = true
fallMul = 6.0           （速度 ×6 加速下落）
fadeMul = 1.8           （淡出速度 ×1.8）
屏幕上半部分: 保持可见 → 让用户看到加速下落过程
屏幕下半部分: curOpacity = baseOpacity × (1 - y/(vh+340))
3秒后: 彻底淡出（不再参与循环）
```

#### 命中叶子（六角密堆积 + 快速出现）

```
位置目标（六角密堆积，从底部开始堆）:
  baseY = vh - 160            ← 底部上移 160px（避开 Dock）
  行 r = 0,1,2...:
    每行偏移: even行 0, odd行 w/2  （六角交错）
    列 c = 0,1,2...:
      x = c × (w × 0.6) + 左右居中补正
      y = baseY - r × (h × 0.866) - h × 0.6
达到目标:
  lerp: 每帧 x/y 向 hexTarget 靠拢 (k = 0.08)
  rot: 向 0° 靠拢
  scale: 目标 0.6（堆积时缩小，留出空间）
  误差 < 2px → hexSettled = true
时间约束: 搜索开始后 3 秒内完成堆积
频率: matchedMotions 优先出现（未命中释放被延迟）
```

### 8.7 风力系统

```
WIND_CHANGE_INTERVAL = 7000ms   (每 7 秒换一次风向目标)
windTarget = rand(-0.8, 0.8)    (负=吹左，正=吹右)
wind = lerp(wind, windTarget, 0.008)  (极慢缓动)
对每片叶子的 x 影响:
  windX = wind × (2 + m.layerIdx) × 4    (近景受风力更大，真实)
```

---

## 9. 交互状态机

### 9.1 单片叶子的状态

```
stateDiagram-v2
  [*] --> Waiting: releaseDelay
  Waiting --> Falling: delay=0
  Falling --> FadingIn: fadeInT>0
  FadingIn --> Falling: fadeInT=0

  Falling --> Frozen: mouseenter
    note: 停住 + lerp摆正 + 放大×1.18 + z→9999 + 显示操作按钮
  Frozen --> Falling: mouseleave

  Frozen --> Expanding: click
    note: transition 200ms 到屏幕正中 + 全屏覆盖
  Expanding --> Frozen: 用户关闭详情 overlay
    note: 叶子平滑回到原位

  Falling/Frozen --> FlyAway: 点击删除 + 确认
    note: 向右上方旋转飘出 + 淡出 1.2秒
  FlyAway --> [*]: 动画结束 (detachMotion 删除)

  Falling --> SearchHidden: 搜索未命中
    note: fallMul×6 + 淡出
  Falling --> HexPacking: 搜索命中
    note: lerp 到六角堆积目标
  HexPacking --> HexSettled: 误差<2px
```

### 9.2 Dock 的状态

```
Normal:
  所有 dock-item 50×50 / primary 56×56
  鼠标在 Dock 上:
    计算每个 dock-item 到鼠标的距离 → 高斯分布影响力
    鼠标正下方 item: width/height → 80×80 (primary→88×88)
    两侧 item: 随距离递减，flex 自动让位（MAC 磁吸）
  鼠标离开 Dock:
    所有 item 0.18s 缩回基础尺寸
```

### 9.3 搜索状态机（重点：打字不乱闪）

```
关键变量: prevHasQuery (跟踪搜索状态变化)

输入字符:
  hasQuery = !!currentQuery.trim()
  justStarted = hasQuery && !prevHasQuery

  仅当 justStarted（"无→有"切换）时:
    - 记录 searchStartTime = now
    - 未命中叶子重置 y = -rand(50,250)

  否则（继续打字 or 空格 or 无→无）:
    - 不重置任何叶子位置

更新 matched/unmatched 分类 + 高亮
prevHasQuery = hasQuery
```

### 9.4 新增/编辑/删除后的状态（**绝对不重建舞台**）

| 操作 | 动作 | 其他叶子影响 |
|------|------|-------------|
| 新建卡片 | `appendLeaf(card)`：新叶从顶部进入，带 releaseDelay | **继续自由下落**，位置不变 |
| 编辑卡片 | `updateLeafCard(card)`：替换该叶的 DOM 内容（title/body/颜色/叶型），保留位置+transform+动画状态 | **继续自由下落**，位置不变 |
| 删除卡片 | `flyAwayAndDelete()` → 飘走 1.2s → `detachMotion(id)`：从 motions 和 DOM 移除该叶 | **继续自由下落**，位置不变 |
| 切换排序/氛围 | `renderStage()`：必须重建（因为层分配变了） | 全部重新进入（带 stagger 错开） |

---

## 10. 响应式适配

### 10.1 断点 @media (max-width: 720px)

```css
:root {
  --leaf-w: 240px;
  --leaf-h: 288px;     /* 保持 5:6 */
}
.leaf-title { font-size: 20px; }
.leaf-body  { font-size: 13px; }

.dock-panel { gap: 8px; padding: 8px 10px; }       /* Dock 更紧凑 */
.top-badge { font-size: 12px; padding: 6px 10px; }
.detail-modal, .edit-modal { padding: 24px 18px; }  /* 弹窗更紧 */
```

> **注意**：Dock 的磁吸放大效果在小屏下自动缩小（因为基础尺寸小了），不需要额外调整公式。

---

## 11. 背景层 SVG 素材

### 11.1 SVG 容器

```
<svg viewBox="0 0 1600 900" preserveAspectRatio="xMidYMax slice">
```

- 宽度基准 1600px、高度基准 900px
- `xMidYMax slice`：下对齐（保证地平线始终在底部）、裁掉超出部分

### 11.2 元素清单（按 z 从后到前）

| 元素 | class | 描边颜色 | 不透明度 | 位置 |
|------|-------|---------|---------|------|
| 远山 | scenery-line | `--line` 主棕 | 0.55 | y=640~720，双峰曲线 |
| 近山 | scenery-line soft | `--line` | 0.32 | y=740~790，双 Q 曲线 |
| 远树（左）×2 | scenery-line soft | `--line` | 0.32 | x=120/180，y=720/740 |
| 远树（右） | scenery-line soft | `--line` | 0.32 | x=1380，y=710 |
| 云 ×2 | scenery-line cloud | `--line` | 0.28 | x=300 y=180 / x=1080 y=140 |
| 芦苇 ×2 丛 | scenery-line reed | `--line` | 0.50 | x=820 y=820 / x=1180 y=830 |
| 地平线 | scenery-line soft | `--line` | 0.32 | y=820，全宽横线 |

### 11.3 云的路径公式（可复用）

```
d="M0,0
   q-18,-20 6,-28     ← 左凸
   q8,-22 32,-12      ← 左上
   q18,-18 38,4       ← 顶部
   q22,-6 26,16       ← 右上
   q16,12 -2,20 Z"    ← 右凸收
```

### 11.4 天空渐变（3 段式）

```css
.bg-layer.sky {
  background: linear-gradient(
    180deg,
    var(--sky-1) 0%,    /* 顶部金 */
    var(--sky-3) 55%,   /* 中部柔 */
    var(--sky-2) 100%   /* 底部霞 */
  );
  transition: background 0.8s ease;  /* 切换氛围时平滑过渡 */
}
```

---

## 12. 动效编排节奏表

### 12.1 进入类动效

| 动效 | 时长 | 缓动 | 起点 → 终点 |
|------|------|------|-------------|
| 落叶顶部缓入 | 600ms | easeOutCubic | opacity 0 → baseOpacity |
| 初始化错开释放 | 0.7 × fallMs | — | 每叶 releaseDelay 递增 |
| 搜索条展开 | 300ms | cubic-bezier(.2,.7,.3,1.2) | width 0→220, opacity 0→1 |
| 排序/氛围气泡弹出 | 200ms | cubic-bezier(.2,.7,.3,1.2) | 下8px→0, opacity 0→1 |
| 详情/编辑/删除弹窗 | 300ms | cubic-bezier(.2,.7,.3,1.2) | 弹出 popIn |
| 遮罩层 | 250ms | ease | opacity 0→1 |
| Toast 弹入 | 300ms | cubic-bezier(.2,.7,.3,1.2) | 下14px→0, opacity 0→1 |
| 管理面板滑入 | 300ms | cubic-bezier(.2,.7,.3,1.2) | translateX 100%→0 |

### 12.2 交互类动效

| 动效 | 时长 | 缓动 | 说明 |
|------|------|------|------|
| Dock 控件放大/缩回 | 180ms | cubic-bezier(.2,.7,.3,1.2) | width/height 变化 |
| 落叶 hover 摆正/放大 | ~200ms | lerp k=0.18 (~11帧稳定) | rot→0, scale→1.18 |
| 落叶 hover 离开恢复 | 立即 | — | 跟随自然物理继续下落 |
| 落叶全屏展开 | 200ms | cubic-bezier(.2,.7,.3,1.2) | transition |
| 落叶详情关闭回原位 | 200ms | ease | transition |
| 删除飘走 | 1200ms | 线性+重力加速度 | x+, y先-后+, rot+, opacity- |
| 搜索未命中加速坠落 | <3000ms | 线性+淡出 | fallMul×6 |
| 搜索命中六角堆积 | <3000ms | lerp k=0.08 | 目标 <2px 误差 |
| 风切换缓动 | 7000ms | lerp k=0.008 (极慢) | wind→windTarget |

### 12.3 微交互细节

| 元素 | hover 效果 | 时间 |
|------|-----------|------|
| Dock 关闭按钮 × | rotate(90deg) + 变红 | 0.15s + 0.2s |
| 操作按钮 | scale(1.12) + 更黑背景 | 0.15s + 0.2s |
| primary-btn | translateY(-1px) + 更深阴影 | 0.15s + 0.2s |
| danger-btn | translateY(-1px) | 0.15s |
| m-card | translateY(-2px) + 更浅背景 | 0.15s + 0.2s |
| preset-color | scale(1.15) + 选中描边 | 0.15s |
| leaf-type-btn | scale(1.1) + 选中描边 | 0.15s + 0.2s |

---

## 13. 复现检查清单

新建类似页面时，按此清单逐项验证，确保风格一致：

### 13.1 视觉层
- [ ] 三套氛围色 token 全部对应定义（sky-1~3 / line / paper-1~2 / ink-1~3 / accent-1~2 / danger / overlay / glass-bg + border）
- [ ] 天空渐变 3 段，transition 0.8s
- [ ] 背景 SVG 远山/近山/远树/云/芦苇/地平线，class 和 opacity 正确
- [ ] 叶柄固定棕色（`--stem-color: #5d3a1a`），叶身染色，叶脉浅金半透
- [ ] 12 色真实秋色调色板（不含刺眼纯黄）
- [ ] 叶身 viewBox 统一 0 0 100 120（5:6）
- [ ] 叶片尺寸 300×360（移动端 240×288），比例严格 5:6
- [ ] 文字区定位 `top:25% bottom:28% left:22% right:22%`，不超出叶身
- [ ] 三层景深（far/mid/near）参数：scale 0.65/0.85/1.05、opacity 0.55/0.82/1.00、speed 0.18/0.32/0.50、swayAmp 12/28/46
- [ ] 玻璃材质：backdrop-filter blur(≥16px) + 半透明底色 + 白色高光边 + inset 高光/暗边

### 13.2 组件层
- [ ] Dock 胶囊：glass T1 级（blur 28px saturate 180%）+ 三层阴影
- [ ] Dock 磁吸推开：width/height transition（非 scale），hover 80×80，主按钮 88×88
- [ ] Dock label 气泡：hover 时上弹 2px 出现
- [ ] 顶部徽标：玻璃 T2 级，999px 胶囊
- [ ] 落叶操作按钮：T4 微玻璃（黑遮罩 + blur 6px）+ 悬停更黑+缩放
- [ ] 弹窗：overlay blur 8px + popIn cubic-bezier(.2,.7,.3,1.2)
- [ ] 管理面板：右侧滑入 slideIn 0.3s，grid auto-fill 240px
- [ ] Toast：999px 玻璃胶囊，从下弹入 0.3s
- [ ] 关闭按钮 ×：悬停变红 + rotate(90deg)
- [ ] 删除确认：有 leafFloat 动画 + 二次确认文案 "让它飘走" / "留下它"

### 13.3 动效层
- [ ] 三层景深层视差正确（近快远慢、近大远小、近实远虚）
- [ ] 风力缓动每 7 秒换目标，缓动系数 0.008
- [ ] 左右边缘斥力：margin 90px，系数 0.35（平方力）
- [ ] **上边框绝对不加斥力**
- [ ] 叶片间斥力：minDist 165px × scale，系数 1.4（平方力），y 推 70%
- [ ] 顶部缓入 600ms easeOutCubic
- [ ] 初始化 stagger：`(i/n) × fallMs × 0.7`，`releaseDelay` 期间完全隐藏
- [ ] 回收后重新进入：interval 在 `[400ms, 2500ms]` 内与可见数成反比

### 13.4 交互层
- [ ] 悬停：frozen + lerp 摆正 rot→0 + scale×1.18 + z→9999 + 操作按钮出现
- [ ] 悬停离开：立即恢复自然物理，不强制重置
- [ ] 点击展开：origin 0 0 定位 → x=vw/2 - leafW/2, y=vh/2 - leafH/2 → scale 覆盖全屏 + transition 200ms → 720ms 后弹详情
- [ ] 关闭详情：叶子平滑回原位，移除 expanding class
- [ ] 删除：flyAway 1200ms 右上方旋转淡出 → 仅 detachMotion 不重建舞台
- [ ] 新增：appendLeaf 仅追加新叶，**其他叶子继续下落位置不变**
- [ ] 编辑：updateLeafCard 仅替换该叶 DOM 内容，**保留位置和动画状态**
- [ ] 搜索打字：仅"无→有"切换时重置未命中 y，继续打字不重置（不乱闪）
- [ ] 搜索未命中：fallMul×6 加速下落 + 淡出，上半屏保持可见（不直接消失）
- [ ] 搜索命中：3 秒内 lerp 到六角堆积位，底部上移 160px 避开 Dock
- [ ] 所有重建舞台的情况：仅"切换排序/切换氛围"（层分配变化必须重建）

### 13.5 存储层
- [ ] 卡片：`localStorage` 键 `inspiration_cards_v2`
- [ ] 设置：`localStorage` 键 `inspiration_settings_v2`
- [ ] 首次无数据时播种 6 张示例卡

---

## 附录 A：CSS 变量完整索引表

| 变量 | dusk 值 | 说明 |
|------|---------|------|
| `--leaf-w` | 300px | 叶片宽（:root） |
| `--leaf-h` | 360px | 叶片高（:root） |
| `--radius` | 16px | 标准圆角（:root） |
| `--radius-sm` | 10px | 小圆角（:root） |
| `--shadow-soft` | rgba(80,40,10,.18) 0 8 28px | 软阴影（:root） |
| `--shadow-deep` | rgba(80,40,10,.28) 0 18 48px | 深阴影（:root） |
| `--stem-color` | #5d3a1a | 叶柄棕色（:root，不变） |
| `--sky-1 / sky-2 / sky-3` | #f6d365 / #fda085 / #f8b97a | 天空渐变 3 段 |
| `--line` | #8b5a2b | 主描线（远山） |
| `--line-soft` | rgba(139,90,43,.45) | 副描线（近山/云） |
| `--paper` | #faf0d7 | 纸色（弹窗底色） |
| `--paper-2` | #f1e0b2 | 次级纸色（悬停态） |
| `--paper-border` | rgba(139,90,43,.22) | 纸色描边 |
| `--ink / ink-2 / ink-3` | #3a2817 / #7b5a3b / #a08566 | 三级文字层次 |
| `--accent / accent-2` | #d94e2f / #e5b93a | 主副强调色（枫红/金） |
| `--danger` | #a9331a | 危险色（暗红） |
| `--overlay` | rgba(120,70,30,.45) | 遮罩透明度 |
| `--glass-bg` | rgba(250,240,215,.35) | 玻璃底色（纸色 35%） |
| `--glass-border` | rgba(255,255,255,.30) | 玻璃高光边（白 30%） |

## 附录 B：关键 JS 数值调参索引表

| 常量 | 值 | 作用 |
|------|-----|------|
| `LAYERS[0-2].scale` | 0.65 / 0.85 / 1.05 | 远中近景视觉大小比例 |
| `LAYERS[0-2].speed` | 0.18 / 0.32 / 0.50 | 远中近景下落速度（px/帧） |
| `LAYERS[0-2].swayAmp` | 12 / 28 / 46 | 摆动幅度 px（近景摆更大） |
| `HOVER_SCALE` | 1.18 | 悬停放大倍数 |
| `WIND_CHANGE_INTERVAL` | 7000 ms | 换风向周期 |
| `LERP_K（悬停）` | 0.18 | 摆正/放大插值系数 |
| `LERP_K（六角堆积）` | 0.08 | 搜索堆积向目标靠拢系数 |
| `edgeMargin` | 90 px | 边缘斥力作用范围 |
| `edgeK` | 0.35 | 边缘斥力系数 |
| `pairMinDist` | 165 px × scale | 叶片间斥力作用距离 |
| `pairK` | 1.4 | 叶片间斥力系数 |
| `fadeInT` 初始 | 600 ms | 顶部缓入时间 |
| `fallMul（搜索未命中）` | 6.0 | 搜索未命中加速倍数 |
| `搜索堆积 bottomY` | vh - 160 | 六角堆积底部上移距离（避开 Dock） |
| `flyAway 总时长` | 1200 ms | 删除飘走动画总时长 |
| `expand → 详情 delay` | 720 ms | 叶子展开全屏后弹详情的延迟 |
| `释放 stagger 系数` | 0.7 × fallMs | 初始化错开释放的系数上限 |
| `回收 re-enter 间隔` | max(400ms, min(2500ms, 8000/n)) | 回收后重新进入错开间隔 |

---

> 文档结束。若需复现此主题，请逐条对照 13. 复现检查清单实现，保证视觉与交互细节一致。
